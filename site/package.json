{
  "name": "weave-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx velojs dev",
    "build": "npx velojs build --static && cd dist && zip -r ../site.zip . && cd .. && echo 'site.zip ready'"
  },
  "dependencies": {
    "@mauroandre/velojs": "^0.0.36"
  },
  "devDependencies": {
    "@vanilla-extract/css": "^1.21.1",
    "@vanilla-extract/vite-plugin": "^5.2.3",
    "marked": "^18.0.5",
    "shiki": "^4.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.2"
  }
}
